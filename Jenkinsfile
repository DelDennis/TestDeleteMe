pipeline {
    agent any
    stages {
        stage('Testing') {
            steps {
                echo 'Test Successful from Master'
            }
        }
        stage('Build') {
            steps {
                echo 'Building Docker Image'
            }
        }
    }
}
